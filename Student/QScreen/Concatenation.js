
import React,{useState,useEffect}from 'react';
import {
    StyleSheet,
    Text,
    View,
    Image,
    TextInput,
    Button,
    TouchableOpacity,
    Modal,
    KeyboardAvoidingView,
    ScrollView,
    Switch,CheckBox,Alert,
  } from "react-native";

const Concatenation=(props)=>{
    return(
        <View>
        <Text>
       
        the purpose of concatenation operator is to combine two columns into a single column in the output only
        </Text>
    </View>
    )
}
export  default Concatenation;